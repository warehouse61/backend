
const faker = require('faker');

exports.seed = function(knex) {

  // Deletes ALL existing entries
  
      // Inserts seed entries
      // return knex('auction_comments').insert([
      //   {userId: 1, comment: faker.lorem.sentences(3), auctionId: 1},
      //   {userId: 1, comment: faker.lorem.sentences(2), auctionId: 1},
      //   {userId: 3, comment: faker.lorem.sentences(1), auctionId: 2}
      // ]);
    
};
